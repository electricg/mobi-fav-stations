/* jshint unused:false */
/* eslint no-unused-vars: "off" */
'use strict';

const VERSION = '0.1';
const NAMESPACE = 'mobiFavStations';

const FEATURES = {
  offline: false,
  local: false,
};

Object.freeze(FEATURES);
